
import { Settings } from "@/types/settings";

// Le cache est géré par React Query, donc nous n'avons plus besoin
// de maintenir notre propre cache ici
export function clearSettingsCache() {
  // Cette fonction reste vide mais est conservée pour compatibilité
  // avec le code existant
  console.log("Settings cache cleared");
}

